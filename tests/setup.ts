/**
 * tests/setup.ts
 *
 * Global Vitest setup file.
 * Extends the built-in expect with @testing-library/jest-dom custom matchers
 * (e.g. toBeInTheDocument, toHaveValue, toBeDisabled).
 */
import "@testing-library/jest-dom";
